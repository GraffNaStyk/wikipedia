{% extends layout %}

{% block content %}
<div class="table-responsive">
  <table class="table table-striped table-bordered table-dark sortable">
    <thead>
      <tr>
        <th>Number</th>
        <th class="no-sort">Name</th>
        <th>
          Status points
          &nbsp;
          <b style="color: green;">
            {{ points }}
          </b>
        </th>
        <th class="no-sort">
          description
        </th>
      </tr>
    </thead>
    <tbody>
    {% for achievement in achievements %}
      <tr>
        <td>
          <b style="color: #04b804">{{ achievement.number }} </b>
        </td>
        <td>
          <b>{{ achievement.name }}</b>
        </td>
        <td>
          {{ achievement.status_points }}
        </td>
        <td>
          {{ achievement.description }}
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</div>
{% endblock %}
